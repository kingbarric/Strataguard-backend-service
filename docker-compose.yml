version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: estatekit-postgres
    environment:
      POSTGRES_DB: estatekit
      POSTGRES_USER: estatekit
      POSTGRES_PASSWORD: estatekit
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U estatekit"]
      interval: 10s
      timeout: 5s
      retries: 5

  keycloak:
    image: quay.io/keycloak/keycloak:24.0.5
    container_name: estatekit-keycloak
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://host.docker.internal:5432/estatekit
      KC_DB_USERNAME: estatekit
      KC_DB_PASSWORD: estatekit
      KC_HOSTNAME: localhost
      KC_HOSTNAME_PORT: 9090
      KC_HTTP_ENABLED: "true"
      KC_HOSTNAME_STRICT_HTTPS: "false"
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    command: start-dev
    ports:
      - "9090:8080"
    # depends_on:
    #   postgres:
    #     condition: service_healthy

volumes:
  postgres_data:
